AI_GATEWAY_API_KEY=
MOLIO_MCP_URL=https://mcp.molio.dk/api/mcp
# Optional dev override — use a raw Supabase access_token JWT only.
# Prefer clicking «Forbind Molio» in the app (OAuth with auto-refresh).
MOLIO_MCP_TOKEN=
